<!DOCTYPE html>
<html>
<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
	<title>Добрая Машина Правды №4</title>
	
	<link rel="icon" type="image/x-icon" href="{info.domain}/favicon.ico" />
	<link rel="shortcut icon" type="image/x-icon" href="{info.domain}/favicon.ico" />
	<script type="text/javascript">
		var g_user,
			opener;
		{?user}
		g_user = {
			name:'{user.name}',
			status:'{user.status}'
		};
		{/user}

		opener = window.opener || window.top;

		if (g_user) {
			opener.loginCallback && opener.loginCallback(g_user);
		} else {
			opener.location = '{info.domain}';
		}
		
		!opener && (window.location = '{info.domain}');
	</script>
</head>
<body>
Подождите, пожалуйста...
</body>
</html>